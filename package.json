{
  "name": "immich-plugin-no-exif-date-fallback",
  "version": "0.1.0",
  "description": "Immich plugin that repairs assets dated 1970-01-01 by recovering the date the file itself carries.",
  "type": "module",
  "license": "AGPL-3.0-only",
  "scripts": {
    "bundle": "esbuild src/index.ts --bundle --format=cjs --target=es2020 --outfile=dist/bundle.js",
    "build": "npm run bundle && extism-js dist/bundle.js -i plugin.d.ts -o dist/plugin.wasm",
    "typecheck": "tsc --noEmit",
    "test": "node --test \"test/*.test.ts\""
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.28.0",
    "typescript": "^5.6.0"
  }
}
